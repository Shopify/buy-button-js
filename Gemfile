source 'https://rubygems.org'

# Jekyll for the local docs preview server (`pnpm run docs`) and the
# GitHub Pages site built from /docs on main. No Jekyll plugins are used,
# so the default gem is sufficient.
gem 'jekyll', '~> 4.4'
