const toggleTemplates = {
  title: '<h5 class="{{data.classes.title}}">{{data.text.title}}</h5>',
  count: '<div class={{data.classes.count}}">{{data.count}}</div>',
};

export default toggleTemplates;
